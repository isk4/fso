{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "isk4",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'part3(phonebook): build UI' && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.1.2",
    "morgan": "^1.10.1"
  }
}
